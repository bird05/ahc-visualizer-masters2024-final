// 入力フォーマット
export type Input_type = {
  is_valid: boolean;
  N: number;
  M: number;
  a: number[];
  b: number[];
}
// 出力フォーマット
export type Output_type = {
  is_valid: boolean;
  c: number[];
  d: number[];
  V: number;
  t: number[];
  r: number[];
}